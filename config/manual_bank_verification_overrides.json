{
  "_purpose": "RETIRED 2026-09-08. Was a TEMPORARY stand-in for com_bank_verification on named vendors only, while ongrid.bank-verification.verify returned a hard 403 (API key not authorized) -- see vdd/finoscale_api/client.py. The founder granted proper API role/access on 2026-09-08; a fresh, non-cached call for MVIKAS (account 393033000000006 / IFSC IOBA0003930) now returns a genuine live success -- 'Bank Account details verified successfully.', bank_account_data.name='MVIKAS TECHNOLOGIES PRIVATE LIMITED' (exact match to the legal name), real request_id/transaction_id (not a seeded stub). Removed both vendor entries -- do not re-add without re-confirming the endpoint has actually broken again first. Note: this fix was confirmed for ongrid/bank-verification/verify specifically; probe42 fetch-by-page('compliance') and digitap/pan-and-gst were still failing (403 / Http Exception) in that same fresh test run, so they remain separately blocked."
}
